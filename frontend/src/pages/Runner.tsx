import {Header} from "../components/Header";
import {Toolbar} from "../components/Toolbar";

export const Runner = () => {
    return (
        <>
            <Header page={"runner"}/>
            <Toolbar page={"runner"}/>
        </>
    )
}